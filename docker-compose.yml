services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: nirit
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: elasticash
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
